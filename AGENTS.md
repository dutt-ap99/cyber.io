Stack: React with Vite, strict TypeScript, Express backend. Use pnpm exclusively.
Database: We use Prisma ORM with a local SQLite database for development. Rely strictly on schema.prisma for database structure.
Workflow: Do not attempt to build frontend UI and backend API routes in the same task. Keep PRs modular.
